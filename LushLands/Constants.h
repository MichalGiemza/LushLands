#pragma once

// Simulation
const int TicksPerSecond = 24;
const int FramesPerSecond = 60;

// World
const int worldHeight = 64; // Y coordinates up to this value

// Chunks
const int chunkSize = 32; // Square for X and Z coordinates up to this value per chunk
const int chunkRenderDistance = 2;

// System
const int representationComaValue = 100;
const int tileSize = 32;
